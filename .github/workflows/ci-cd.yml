name: Build and Push Node.js Docker Image to JFrog

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: uros-simeunovic/vite-cypress/.github/workflows/main.yml@main

